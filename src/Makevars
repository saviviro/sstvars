# Default settings for all platforms
PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS)

# Additional settings for Unix-like systems (Debian, etc.)
ifeq($(UNAME_S), Linux)
  PKG_LIBS += $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
endif
